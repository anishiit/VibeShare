<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
<div class="profile flex justify-between">
    <h3 class="text-3xl"><span class="font-bold text-gray-400">Hello ,</span>  <%= user.name %> 👋🏻 </h3>
    <div>
        <a class="bg-blue-500 rounded-md text-sm p-2" href="/home">Home</a> 
        <a class="bg-red-500 rounded-md text-sm p-2" href="/logout">Logout</a></div>
</div>
<div class="postCreation py-2">
    <h5 class="text-gray-500 py-3">create your post</h5>
    <form method="post" action="/post">
<textarea placeholder="what's on your mind ?" class="w-1/2 p-3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
<input class="px-3 py-2 bg-blue-500 block rounded-md mt-2 text-sm" type="submit" value="Create new post">
    </form>
</div>
<div class="posts mt-10">
    <h3 class="text-zinc-400">Your Posts</h3>
    <div class="postcontainer mt-5 flex  gap-2  flex-wrap">
    <% user.posts.reverse().forEach(function(post){%>
        <div class="post mb-2 w-1/3 p-4 border-[1px] border-zinc-700 bg-zinc-800 rounded-md">
            <h4 class="text-blue-500 mb-3">@<%= user.username %></h4>
            <p class="text-sm text-gray-400 tracking-tight"><%= post.content %></p>
            <div class="btns flex gap-4 mt-5">
                <a href="" class="text-blue-700">Like</a>
                <a href="" class="text-zinc-500">Edit</a>
            </div>
                    </div>
   <% }) %>
    </div>
</div>
    </div>
</body>
</html>